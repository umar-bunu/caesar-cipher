#include <iostream>
using namespace std;

char* encrypt(char* msg);
char* decrypt(char* msg);
int main() {

    int shiftCount;
    cout<<endl<<"Enter shift count: ";
    cin >>shiftCount;


    return 0;
}